import { FeaturePlaceholder } from "@/components/layout/feature-placeholder";

export default function PromptsPage() {
  return (
    <FeaturePlaceholder
      description="Prompt 库将在后续迭代中开放，当前可先浏览首页内容流。"
      title="Prompt 库"
    />
  );
}
