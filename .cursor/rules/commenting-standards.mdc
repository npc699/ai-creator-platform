---
description: 项目代码注释规范
alwaysApply: true
---

# 注释规范

**原则：** 注释解释「为什么」，不重复代码已表达的内容。好代码应 mostly 自解释；只在读者容易误解或踩坑处补充。

## 何时写

| 场景 | 说明 |
|------|------|
| 安全边界 | 认证、鉴权、所有权校验、开放重定向防护、密钥/会话处理 |
| 非显然业务规则 | 状态机分支、Feed 排序/缓存策略、审核拦截条件 |
| 模块边界 | 文件顶部的职责说明；Client/Server 导入约束（如 `server-only`、`@/lib/auth/client`） |
| 失败处理 | 为何返回特定状态码、为何静默失败 vs 抛错、删号后会话失效等 |
| 外部约定 | Cron 调用方式、环境变量依赖、与第三方 API 的隐含约束 |

**不必写：** getter/setter、 obvious 的 UI 结构、逐行流程复述、用户未改动的旧代码（见 `coding-behavior.mdc`）。

## 写法

- 语言统一使用**中文**。
- 单行用 `//`，导出函数/路由 handler 可用 `/** ... */` 一行摘要。
- 注释紧贴所解释代码；逻辑变更时同步更新，避免过期注释。
- 优先写约束与意图，少写操作步骤。

## 示例

```typescript
// ❌ 重复代码
// 设置 isSubmitting 为 true
setIsSubmitting(true);

// ❌ 无信息增量
// 查询用户
const user = await prisma.user.findUnique(...);

// ✅ 解释为什么
// redirect: false 让页面自行处理成功/失败，避免 Auth.js 默认整页跳转。
const result = await signIn("credentials", { redirect: false });

// ✅ 安全边界
// 统一错误文案，不区分「账号不存在」与「密码错误」，降低账号枚举风险。
setError("账号或密码不正确");

// ✅ 模块边界
// 仅供 Client Component 使用，勿在此导出依赖 Prisma/Node 内置模块的内容。
```

## 本项目重点区域

编辑以下路径时，优先检查信任边界与失败分支是否已有短注释：

- `lib/auth/`、`proxy.ts` — 会话、回跳、identifier 解析
- `app/api/**` — 鉴权、所有权、HTTP 语义（404 vs 403 vs null）
- `lib/feed/`、`lib/review/` — 排序、缓存、审核决策
- `lib/db/` — 连接单例、降级策略
